[English](../README.md) · [العربية](README.ar.md) · [Español](README.es.md) · [Français](README.fr.md) · [日本語](README.ja.md) · [한국어](README.ko.md) · [Tiếng Việt](README.vi.md) · [中文 (简体)](README.zh-Hans.md) · [中文（繁體）](README.zh-Hant.md) · [Deutsch](README.de.md) · [Русский](README.ru.md)


# lazylanguagelearner

[![License: Apache-2.0](https://img.shields.io/badge/License-Apache%202.0-blue.svg)](LICENSE)
![Python](https://img.shields.io/badge/Python-3.10%2B-3776AB?logo=python&logoColor=white)
![Status](https://img.shields.io/badge/Status-Script--Driven-orange)
![Web](https://img.shields.io/badge/Web-Tornado-5C2D91?logo=tornado)
![AI](https://img.shields.io/badge/OpenAI-API-10A37F?logo=openai&logoColor=white)

تعلّم اللغات بطريقة كسولة.

## 🌍 نظرة عامة

LazyLanguageLearner هو سير عمل لتعلّم اللغات مبنيّ على Python ويجمع بين:

- جلب ملفات PDF الخاصة بمحتوى دورات Rosetta Stone.
- تحليل ملفات PDF واستخراج الجمل إلى مجموعات بيانات CSV.
- تحويل متعدد اللغات للجمل مدعوم بـ OpenAI مع أزواج صوتية وتخزين مؤقت محلي على القرص.
- تطبيق ويب خفيف باستخدام Tornado لعرض نصوص متعددة اللغات مع تعليقات صوتية بأسلوب ruby.

المستودع الحالي يعتمد على السكربتات (وليس مُحزَّمًا بعد كوحدة pip)، مع تضمين ملفات البيانات ودفاتر الملاحظات مباشرة داخل المستودع.

## ✨ الميزات

- تنزيل ملفات PDF لدورات اللغة من الروابط المضمَّنة في `rs_html.py` (عبر `download_course_text.py`).
- استخراج بيانات الأقسام/الجمل من ملفات PDF إلى CSV منظّم (`pdf_to_csv.py`, `language_extraction.py`).
- تخزين بيانات طلبات/استجابات OpenAI مؤقتًا في `cache/*.json` لتقليل الاستهلاك المتكرر للـ API (`openai_request.py`).
- تحليل استجابات الذكاء الاصطناعي إلى JSON مع منطق إعادة المحاولة وأخطاء تحليل JSON مخصّصة.
- تقديم كتل الجمل متعددة اللغات من `translations.json` عبر Tornado (`app.py` + `templates/index.html`).
- يتضمن تطبيعًا صوتيًا للغة اليابانية (من `katakana` إلى `hiragana`) قبل العرض.

## 🗂️ هيكل المشروع

```text
.
├── README.md
├── LICENSE
├── app.py
├── download_course_text.py
├── rs_html.py
├── pdf_extraction.py
├── pdf_to_csv.py
├── language_extraction.py
├── openai_request.py
├── multilingual_sentence.py
├── translations.json
├── japanese_language_data.csv
├── japanese_language_data copy.csv
├── processed_words.csv
├── templates/
│   └── index.html
├── cache/
│   └── *.json
├── i18n/
│   └── (currently empty)
└── *.ipynb
```

## ✅ المتطلبات المسبقة

الافتراضات (لأنه لا يوجد حاليًا lockfile أو dependency manifest مُلتزَم في المستودع):

- Python 3.10+ (غالبًا يعمل على إصدارات قريبة؛ مصفوفة الإصدارات المختبرة بدقة غير مُعلنة).
- `pip` و `venv`.
- مفتاح OpenAI API للسكربتات المعتمدة على النماذج.

تبعيّات Python المستنتجة من الـ imports:

| Package | Used by |
|---|---|
| `tornado` | Web server in `app.py` |
| `openai` | API calls in `openai_request.py` |
| `PyPDF2` | PDF parsing in extraction scripts |
| `requests` | PDF downloads in `download_course_text.py` |
| `beautifulsoup4` | HTML parsing in downloader |

## 🛠️ التثبيت

```bash
# from repository root
python3 -m venv .venv
source .venv/bin/activate
pip install --upgrade pip
pip install tornado openai PyPDF2 requests beautifulsoup4
```

## 🚀 الاستخدام

### 1) تنزيل ملفات PDF المصدر

```bash
python download_course_text.py
```

يؤدي هذا إلى إنشاء `downloaded_pdfs/` وحفظ ملفات PDF الخاصة باللغة/الوحدة بداخله.

### 2) استخراج محتوى PDF الياباني إلى CSV

```bash
python pdf_to_csv.py
```

المخرجات الافتراضية من السكربت الحالي: `japanese_language_data.csv`.

### 3) (اختياري) تقطيع نص القسم/الصفحة/الجملة بشكل تفاعلي

```bash
python language_extraction.py
```

يحتوي السكربت على متغيرات أمثلة قابلة للتعديل (`level`, `section`, `sentence_num`) ويطبع النص المستخرج.

### 4) توليد JSON متعدد اللغات باستخدام تدفّق OpenAI

```bash
python multilingual_sentence.py
```

ملاحظات السلوك الحالي:

- تتم معالجة الصف الأول فقط من CSV (`break` داخل الحلقة).
- يشير السكربت حاليًا إلى متغير غير معرّف (`japanese_text`) عند إنشاء الـ prompt، لذلك يحتاج إلى إصلاح بسيط قبل الاعتماد عليه.

### 5) تشغيل تطبيق الويب

```bash
python app.py
```

- يستمع Tornado على المنفذ `7788`.
- افتح في المتصفح: `http://localhost:7788/`.
- ملاحظة: سجل بدء التشغيل يطبع حاليًا `http://localhost:8888` رغم أن الربط الفعلي هو `7788`.

## ⚙️ الإعداد

متغيرات البيئة:

| Variable | Required | Purpose | Current default |
|---|---|---|---|
| `OPENAI_API_KEY` | Yes | Required by `OpenAI()` client initialization | N/A |
| `OPENAI_MODEL` | No | Optional override for chat model | `gpt-4-0125-preview` |

ملفات/مجلدات وقت التشغيل:

- `downloaded_pdfs/`: ينشئه برنامج التنزيل وتستخدمه سكربتات الاستخراج.
- `cache/`: تخزين مؤقت للطلبات/الاستجابات لاستدعاءات OpenAI.
- `translations.json`: مصدر البيانات لعرض واجهة Tornado.

## 🧾 أمثلة تنسيق البيانات

### CSV (`japanese_language_data.csv`)

الرأس المستخدم بواسطة `pdf_to_csv.py`:

```csv
Level,Unit,Section,Sentence No.,Content
```

### JSON (`translations.json`)

تتوقع واجهة الويب مفاتيح لغات تحتوي إدخالات `pairs` تتضمن `part` و `phonetic`:

```json
{
  "ja": {
    "full": "...",
    "pairs": [
      { "part": "日", "phonetic": "ひ" }
    ]
  },
  "en": { "full": "...", "pairs": [] },
  "ar": { "full": "...", "pairs": [] },
  "zh": { "full": "...", "pairs": [] },
  "yue": { "full": "...", "pairs": [] }
}
```

## 🧪 ملاحظات التطوير

- لا يحتوي هذا المستودع حاليًا على `requirements.txt` أو `pyproject.toml` أو سير عمل CI.
- السكربتات مصممة للتشغيل المباشر من جذر المستودع.
- دفاتر الملاحظات الحالية (`*.ipynb`) تبدو استكشافية/موجّهة للنمذجة الأولية.
- ملفات CSV الكبيرة مُدارة بالإصدار مباشرة في Git.
- المجلد `i18n/` موجود وجاهز لنسخ README المترجمة.

## 🩺 استكشاف الأخطاء وإصلاحها

- `ModuleNotFoundError`: ثبّت التبعيات المستنتجة ضمن بيئة افتراضية مفعّلة.
- أخطاء مصادقة `OPENAI`: تأكد من تصدير `OPENAI_API_KEY` في الشِل.
- `FileNotFoundError: downloaded_pdfs`: شغّل أولًا `python download_course_text.py`.
- فشل `multilingual_sentence.py` عند `japanese_text`: استبدل `japanese_text` بـ `content` في بناء الـ prompt.
- التباس منفذ التطبيق: استخدم `http://localhost:7788/` ما لم يتم تغيير `app.listen(...)`.

## 🛣️ خارطة الطريق

الخطوات المحتملة التالية للمشروع:

- إضافة ملف تعريف تبعيات (`requirements.txt` أو `pyproject.toml`).
- إصلاح متغير الـ prompt في `multilingual_sentence.py` وإزالة `break` الخاص بمعالجة صف واحد فقط لتفعيل المعالجة الدفعية.
- مواءمة عنوان URL المطبوع عند بدء Tornado مع المنفذ المرتبط فعليًا.
- إضافة اختبارات لسلوك regex في استخراج PDF ومنطق تحليل JSON/إعادة المحاولة.
- إضافة معاملات CLI للغة/المستوى/المسارات لتقليل التعديلات داخل الملفات.
- تعبئة `i18n/` بملفات README مترجمة.

## 🤝 المساهمة

المساهمات مرحّب بها.

1. اعمل Fork للمستودع.
2. أنشئ فرعًا لميزة جديدة.
3. نفّذ تغييرات مركّزة مع رسائل commit واضحة.
4. افتح Pull Request يوضّح ما الذي تغيّر ولماذا.

إذا عدّلت منطق الاستخراج، أرفق أمثلة لمدخلات/مخرجات لتسهيل المراجعة.

## 🙏 شكر وتقدير

- روابط محتوى دورات Rosetta Stone مضمّنة في `rs_html.py` وتُستخدم كمراجع مصدر لتنزيل ملفات PDF.
- يتم استخدام OpenAI API للتوليد متعدد اللغات وبناء الهيكلة الصوتية.

## 📄 الترخيص

هذا المشروع مرخّص بموجب Apache License 2.0. راجع [LICENSE](LICENSE).

[English](../README.md) · [العربية](README.ar.md) · [Español](README.es.md) · [Français](README.fr.md) · [日本語](README.ja.md) · [한국어](README.ko.md) · [Tiếng Việt](README.vi.md) · [中文 (简体)](README.zh-Hans.md) · [中文（繁體）](README.zh-Hant.md) · [Deutsch](README.de.md) · [Русский](README.ru.md)


# lazylanguagelearner


[![License: Apache-2.0](https://img.shields.io/badge/License-Apache%202.0-blue.svg)](LICENSE)
![Python](https://img.shields.io/badge/Python-3.10%2B-3776AB?logo=python&logoColor=white)
![Status](https://img.shields.io/badge/Status-Script--Driven-orange)
![Web](https://img.shields.io/badge/Web-Tornado-5C2D91?logo=tornado)
![AI](https://img.shields.io/badge/OpenAI-API-10A37F?logo=openai&logoColor=white)

Изучайте язык ленивым способом.

## 🌍 Обзор

LazyLanguageLearner — это Python-ориентированный workflow для изучения языков, который сочетает:

- Загрузку PDF-документов с материалами курсов Rosetta Stone.
- Разбор PDF и извлечение предложений в CSV-датасеты.
- Многоязычное преобразование предложений на базе OpenAI с фонетическими парами и локальным кешированием на диске.
- Легковесное веб-приложение Tornado, отображающее многоязычный текст с ruby-фонетическими аннотациями.

Текущий репозиторий управляется скриптами (пока не оформлен как pip-модуль), а файлы данных и ноутбуки включены прямо в репозиторий.

## ✨ Возможности

- Загружает PDF языковых курсов по ссылкам, встроенным в `rs_html.py` (`download_course_text.py`).
- Извлекает данные разделов/предложений из PDF в структурированные CSV (`pdf_to_csv.py`, `language_extraction.py`).
- Кеширует данные prompt/response OpenAI в `cache/*.json`, чтобы уменьшить повторные обращения к API (`openai_request.py`).
- Разбирает ответы ИИ в JSON с логикой повторных попыток и пользовательскими ошибками парсинга JSON.
- Отдает многоязычные блоки предложений из `translations.json` через Tornado (`app.py` + `templates/index.html`).
- Включает нормализацию японской фонетики (`katakana` в `hiragana`) перед рендерингом.

## 🗂️ Структура проекта

```text
.
├── README.md
├── LICENSE
├── app.py
├── download_course_text.py
├── rs_html.py
├── pdf_extraction.py
├── pdf_to_csv.py
├── language_extraction.py
├── openai_request.py
├── multilingual_sentence.py
├── translations.json
├── japanese_language_data.csv
├── japanese_language_data copy.csv
├── processed_words.csv
├── templates/
│   └── index.html
├── cache/
│   └── *.json
├── i18n/
│   └── (currently empty)
└── *.ipynb
```

## ✅ Требования

Предположения (поскольку lockfile или manifest зависимостей сейчас не закоммичены):

- Python 3.10+ (вероятно, работает и на близких версиях; точная протестированная матрица не заявлена).
- `pip` и `venv`.
- OpenAI API key для скриптов, использующих модель.

Предполагаемые Python-зависимости по импортам:

| Package | Используется в |
|---|---|
| `tornado` | Веб-сервер в `app.py` |
| `openai` | Вызовы API в `openai_request.py` |
| `PyPDF2` | Парсинг PDF в скриптах извлечения |
| `requests` | Загрузка PDF в `download_course_text.py` |
| `beautifulsoup4` | Парсинг HTML в загрузчике |

## 🛠️ Установка

```bash
# from repository root
python3 -m venv .venv
source .venv/bin/activate
pip install --upgrade pip
pip install tornado openai PyPDF2 requests beautifulsoup4
```

## 🚀 Использование

### 1) Загрузка исходных PDF

```bash
python download_course_text.py
```

Создает `downloaded_pdfs/` и сохраняет туда PDF для языков/юнитов.

### 2) Извлечение содержимого японских PDF в CSV

```bash
python pdf_to_csv.py
```

Вывод по умолчанию в текущем скрипте: `japanese_language_data.csv`.

### 3) (Опционально) Интерактивная выборка текста по разделу/странице/предложению

```bash
python language_extraction.py
```

Скрипт содержит редактируемые примерные переменные (`level`, `section`, `sentence_num`) и выводит извлеченный текст.

### 4) Генерация многоязычного JSON через OpenAI workflow

```bash
python multilingual_sentence.py
```

Заметки о текущем поведении:

- Обрабатывается только первая строка CSV (`break` в цикле).
- Сейчас скрипт использует неопределенную переменную (`japanese_text`) при создании prompt, поэтому перед надежным использованием нужен небольшой фикс.

### 5) Запуск веб-приложения

```bash
python app.py
```

- Tornado слушает порт `7788`.
- Откройте в браузере: `http://localhost:7788/`.
- Примечание: в стартовом логе сейчас печатается `http://localhost:8888`, хотя биндинг идет на `7788`.

## ⚙️ Конфигурация

Переменные окружения:

| Variable | Обязательно | Назначение | Текущее значение по умолчанию |
|---|---|---|---|
| `OPENAI_API_KEY` | Да | Требуется для инициализации клиента `OpenAI()` | N/A |
| `OPENAI_MODEL` | Нет | Необязательное переопределение chat-модели | `gpt-4-0125-preview` |

Файлы/директории времени выполнения:

- `downloaded_pdfs/`: создается загрузчиком, используется скриптами извлечения.
- `cache/`: кеш запросов/ответов для вызовов OpenAI.
- `translations.json`: источник данных для рендеринга Tornado UI.

## 🧾 Примеры форматов данных

### CSV (`japanese_language_data.csv`)

Заголовок, используемый `pdf_to_csv.py`:

```csv
Level,Unit,Section,Sentence No.,Content
```

### JSON (`translations.json`)

Web UI ожидает языковые ключи с элементами `pairs`, содержащими `part` и `phonetic`:

```json
{
  "ja": {
    "full": "...",
    "pairs": [
      { "part": "日", "phonetic": "ひ" }
    ]
  },
  "en": { "full": "...", "pairs": [] },
  "ar": { "full": "...", "pairs": [] },
  "zh": { "full": "...", "pairs": [] },
  "yue": { "full": "...", "pairs": [] }
}
```

## 🧪 Заметки по разработке

- В репозитории пока нет `requirements.txt`, `pyproject.toml` или CI workflow.
- Скрипты рассчитаны на прямой запуск из корня репозитория.
- Существующие ноутбуки (`*.ipynb`) выглядят как исследовательские/прототипные.
- Крупные CSV-артефакты версионируются напрямую в Git.
- `i18n/` существует и готова для переведенных вариантов README.

## 🩺 Устранение неполадок

- `ModuleNotFoundError`: установите предполагаемые зависимости в активном виртуальном окружении.
- Ошибки аутентификации `OPENAI`: убедитесь, что `OPENAI_API_KEY` экспортирован в shell.
- `FileNotFoundError: downloaded_pdfs`: сначала запустите `python download_course_text.py`.
- Сбой `multilingual_sentence.py` на `japanese_text`: замените `japanese_text` на `content` при построении prompt.
- Путаница с портом приложения: используйте `http://localhost:7788/`, если `app.listen(...)` не изменен.

## 🛣️ Дорожная карта

Потенциальные следующие шаги для проекта:

- Добавить manifest зависимостей (`requirements.txt` или `pyproject.toml`).
- Исправить переменную prompt в `multilingual_sentence.py` и убрать однострочный `break` для пакетной обработки.
- Синхронизировать URL в стартовом выводе Tornado с фактическим портом биндинга.
- Добавить тесты для поведения regex в извлечении PDF и логики парсинга JSON/повторных попыток.
- Добавить CLI-аргументы для языка/уровня/путей, чтобы снизить число правок прямо в файлах.
- Заполнить `i18n/` переведенными файлами README.

## 🤝 Участие в проекте

Вклад приветствуется.

1. Форкните репозиторий.
2. Создайте feature-ветку.
3. Вносите точечные изменения с понятными сообщениями коммитов.
4. Откройте pull request с описанием того, что изменено и почему.

Если вы меняете логику извлечения, добавьте примеры входных/выходных данных, чтобы упростить ревью.

## 🙏 Благодарности

- Ссылки на материалы курсов Rosetta Stone встроены в `rs_html.py` и используются как исходные ссылки для загрузки PDF.
- OpenAI API используется для многоязычной генерации и фонетического структурирования.

## 📄 Лицензия

Проект распространяется по лицензии Apache License 2.0. См. [LICENSE](LICENSE).
